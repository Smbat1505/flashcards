import { Link } from 'react-router-dom'

import { Typography } from '@/components/ui/typography'

export const Decks2 = () => {
  return (
    <>
      <Link to={'/'}>Decks</Link>
      <Typography variant={'h2'}>Decks 2</Typography>
    </>
  )
}
